# Legacy compatibility file.
#
# The core videoedit package has no mandatory Python runtime dependencies beyond
# the standard library. Prefer package extras instead of this file:
#
#   python -m pip install -e ".[whisper]"
#   python -m pip install -e ".[advanced]"
#   python -m pip install -e ".[ai]"
#   python -m pip install -e ".[ui]"
#   python -m pip install -e ".[cloud]"
#
# From the repository root, use ./src/python in place of ".":
#
#   python -m pip install -e "./src/python[whisper,advanced,ai]"
